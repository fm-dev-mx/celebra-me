#!/bin/sh
# Invoke commitlint through node directly: the corepack `pnpm` shim crashes under
# git-bash (mangled drive path) and `cmd.exe /c` breaks on `/C`-like arguments.
node node_modules/@commitlint/cli/cli.js --edit "$1"
