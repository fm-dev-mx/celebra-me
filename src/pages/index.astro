---
export const prerender = true;
// src/pages/index.astro
import Layout from '@/layouts/Layout.astro';
import Hero from '@/components/home/Hero.astro';
import ProductProof from '@/components/home/ProductProof.astro';
import EventTypeSelector from '@/components/home/EventTypeSelector.astro';
import Services from '@/components/home/Services.astro';
import PhotographicInterlude from '@/components/home/PhotographicInterlude.astro';
import AboutUs from '@/components/home/AboutUs.astro';
import Testimonials from '@/components/home/Testimonials.astro';
import Pricing from '@/components/home/Pricing.astro';
import HowItWorks from '@/components/home/HowItWorks.astro';
import FAQ from '@/components/home/FAQ.astro';
import Contact from '@/components/home/Contact.astro';
import Footer from '@/components/home/Footer.astro';
import { DEMO_SHOWROOM_EVENTS } from '@/data/demo-showroom.data';
import { landingData } from '@/data/landing-page.data';
import { footerData } from '@/data/footer.data';
import '@/styles/landing.scss';

---

<Layout title="Celebra.me | Invitaciones Digitales Premium" className="theme-preset--jewelry-box-landing">
	<div class="landing-sections-wrapper">
		<Hero data={landingData.hero} />
		<EventTypeSelector events={DEMO_SHOWROOM_EVENTS} />
		<ProductProof data={landingData.productProof} />
		<Services data={landingData.services} />
		<PhotographicInterlude />
		<AboutUs data={landingData.about} />
		<HowItWorks data={landingData.howItWorks} />
		<Pricing data={landingData.pricing} />
		<Testimonials data={landingData.testimonials} />
		<FAQ data={landingData.faq} />
		<Contact data={landingData.contact} />
		<Footer data={footerData} />
	</div>
</Layout>
<script>
	import { createIntersectionObserver } from '@/utils/animations';

	const observerOptions = {
		threshold: 0.1,
		rootMargin: '0px 0px -10% 0px',
	};

	createIntersectionObserver('section:not(#nosotros):not(#servicios):not(#pricing), .hero-prime, .services, .about-us', (target) => {
		target.classList.add('is-visible');
	}, observerOptions);

	const html = document.documentElement;

	const syncScrollPadding = () => {
		const currentHeight = getComputedStyle(html).getPropertyValue('--nav-current-height').trim();
		html.style.scrollPaddingTop = currentHeight || '100px';
	};

	syncScrollPadding();
	window.addEventListener('scroll', syncScrollPadding, { passive: true });
</script>
