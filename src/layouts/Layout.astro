---
/** src/layouts/Layout.astro */
import '@/styles/global.scss';

import Header from '@/components/home/HomeHeader.astro';
import Analytics from '@vercel/analytics/astro';
import SpeedInsights from '@vercel/speed-insights/astro';

// Typography: Core 5 Font System (@fontsource includes font-display: swap)
// Display Formal
import '@fontsource-variable/cinzel';
// Display Elegant
import '@fontsource-variable/playfair-display';
// Calligraphy
import '@fontsource/pinyon-script/400.css';
// Body Narrative
import '@fontsource-variable/eb-garamond';
// UI/Functional
import '@fontsource-variable/montserrat';
// Premium Hacienda Display
import '@fontsource-variable/cormorant-garamond';
// Premium Editorial Display
import '@fontsource-variable/bodoni-moda';
// Accent Script (Nautigal)
import '@fontsource/the-nautigal';
// Western/Typewriter (keep for fallback)
import '@fontsource/special-elite/400.css';
import '@fontsource/rye/400.css';

interface Props {
	title: string;
	description?: string;
	image?: string;
	hideHeader?: boolean;
	className?: string;
	isDemo?: boolean;
	themeMode?: 'light' | 'dark';
}

const {
	title,
	description = 'Celebra.me - Eventos inolvidables',
	image = '/images/og-image.jpg',
	hideHeader = false,
	className,
	isDemo = false,
	themeMode = 'dark',
} = Astro.props;

// Environment guard: only load Vercel tooling in production
const isProd = import.meta.env.PROD;

// Ensure full URL for social sharing (absolute URL required by crawlers)
const ogImage = new URL(image, Astro.url);

// Canonical URL (avoid leaking dev localhost into metadata)
const canonicalUrl = isProd ? Astro.url : new URL('https://celebra-me.com/');
---
<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content={description} />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" type="image/x-icon" href="/favicon.ico" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>

		<!-- Open Graph -->
		<meta property="og:type" content="website" />
		<meta property="og:url" content={canonicalUrl} />
		<meta property="og:title" content={title} />
		<meta property="og:description" content={description} />
		<meta property="og:image" content={ogImage} />

		<!-- Twitter -->
		<meta name="twitter:card" content="summary_large_image" />
		<meta property="twitter:url" content={canonicalUrl} />
		<meta property="twitter:title" content={title} />
		<meta property="twitter:description" content={description} />
		<meta property="twitter:image" content={ogImage} />
	</head>
	<body class:list={[className, { 'is-demo': isDemo }]} data-theme-mode={themeMode}>
		{!hideHeader && <Header />}

		<main>
			<slot />
		</main>

		{isProd && <Analytics />}
		{isProd && <SpeedInsights />}
	</body>
</html>
