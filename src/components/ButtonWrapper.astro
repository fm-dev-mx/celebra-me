---
interface Props {
	flex?: string;
	margin?: string;
	align?: string;
	gap?: string;
	className?: string;
}

const props = Astro.props as Props;
const {
	flex = "flex-row",
	margin = "mt-16",
	align = "justify-center",
	gap = "gap-x-6 md:gap-x-12",
	className = "",
} = props;
---

<div class=""></div>
<div class:list={["flex", flex, margin, align, gap, className]}>
	<slot />
</div>
