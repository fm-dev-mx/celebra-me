---
import '@/styles/xv/_section-nav-button.scss';
interface Props {
	href: string;
	class?: string;
	section: string;
}
const { href, class: className, section } = Astro.props;
---

<a href={href} class={`section-nav-button ${className}`}>
	{section}
</a>
