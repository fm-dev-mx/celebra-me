<script type="module">
 import { initializeCardHoverEffects } from "/src/utilities/cardHoverEffects.ts"
  import { initializeCardIntersectionObserver } from '/src/utilities/cardIntersectionObserver.ts';

  document.addEventListener("DOMContentLoaded", function () {
    const isMobile = window.innerWidth < 768;

    if (isMobile) {
		console.log("mobile: " + isMobile);
      initializeCardIntersectionObserver();
    } else {
      initializeCardHoverEffects();
    }
  });
</script>
