---
import Action from "@/components/Action.astro";
import ButtonWrapper from "@/components/ButtonWrapper.astro";
import Card from "@/components/Card.astro";
import Container from "@/components/Container.astro";
import Grid from "@/components/Grid.astro";
import GridRow from "@/components/GridRow.astro";
import Icon from "@/components/Icon.astro";
import IconWrapper from "@/components/IconWrapper.astro";
import Image from "@/components/Image.astro";
import ImageWrapper from "@/components/ImageWrapper.astro";
import Paragraph from "@/components/Paragraph.astro";
import Section from "@/components/Section.astro";
import SectionTitle from "@/components/SectionTitle.astro";
import ShadowContainer from "@/components/ShadowContainer.astro";
import type { AboutData } from "@/config/landing.interface";

interface Props {
	data: AboutData;
}

const { data } = Astro.props;
---

<Section
	id="about-section"
	backgroundImage="/background/pinkStripes.jpg"
	width="w-full"
	relative="relative"
>
	<div class="bg-primary-dark w-full h-1 absolute inset-0 opacity-50"></div>
	<ShadowContainer relative="relative">
		<Container>
			<div id="about-title" class="pt-20"></div>
			<Grid gap="gap-12">
				<GridRow columns={2}>
					<div class="flex flex-col justify-content-center max-w-sm">
						<SectionTitle
							color="text-primary-dark"
							weight="font-bold"
							align="text-center xl:text-right"
							margin="mb-8"
						>
							{data.title}
						</SectionTitle>
						<Paragraph
							size="text-xl"
							align="text-center xl:text-right"
							textColor="text-primary-dark"
							weight="font-normal"
						>
							{data.description}
						</Paragraph>
					</div>
					<div class="lg:flex lg:justify-center xl:items-end">
						<ImageWrapper>
							<Image
								src="about/partyToast.jpg"
								alt="Personas brindando por el festejo"
							/>
						</ImageWrapper>
					</div>
				</GridRow>
				<GridRow columns={3}>
					{
						data.values.map((value) => (
							<Card>
								<IconWrapper background="bg-primary-dark">
									<Icon icon={value.icon} class="mb-4" />
								</IconWrapper>
								<h3 class="text-xl font-semibold mb-2 text-primary-dark">
									{value.name}
								</h3>
								<Paragraph textColor="text-primary">{value.description}</Paragraph>
							</Card>
						))
					}
				</GridRow>
			</Grid>
			<ButtonWrapper gap="gap-x-6 md:gap-x-12" margin="my-16">
				<Action as="a" type="button3" href="#">{data.cta.label}</Action>
			</ButtonWrapper>
			<ButtonWrapper>
				<Action
					as="a"
					type="downButton"
					href="#pricing-section"
					title="Click para ir a la sección de precios"
					iconName="ArrowDownIcon"
				/>
			</ButtonWrapper>
		</Container>
	</ShadowContainer>
</Section>
<div class="bg-primary-dark w-full h-1 opacity-50"></div>
