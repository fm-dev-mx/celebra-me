// File: src/core/types/ui/socialLinkVariants.type.ts

/**
 * Represents the allowed styling variants for social links.
 * These variants are used to apply different styles based on the context
 * (e.g., desktop header, mobile header, or footer).
 */
export type SocialLinkVariants = 'social-desktop-header' | 'social-mobile-header' | 'social-footer';
