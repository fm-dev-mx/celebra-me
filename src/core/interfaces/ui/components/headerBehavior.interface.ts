// src/core/interfaces/ui/components/HeaderBehaviorProps.ts

/**
 * Props interface for HeaderBehavior.
 */
export interface HeaderBehaviorProps {
	headerId: 'main-header'; // ID of the header element to attach event listeners to
}
