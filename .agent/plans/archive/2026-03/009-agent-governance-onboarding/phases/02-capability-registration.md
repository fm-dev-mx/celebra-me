# Phase 02 — Skill and Workflow Metadata Registration

## Objective

Add structured metadata blocks (when_to_use, preconditions, inputs, outputs, related_docs) to all
agent skills and workflows so they can be discovered and loaded programmatically.

## Changes

- 10 skill files: added domain, when_to_use, preconditions, inputs, outputs, related_docs to YAML
  frontmatter
- 5 workflow files: added name and structured metadata blocks to YAML frontmatter

## Status

Complete. Archived as historical record after close-out alignment.
