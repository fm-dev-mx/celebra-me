# Phase 02: Repo & Domain Docs

- Reconcile `README.md`, `scripts/README.md`, and `tests/README.md` with `package.json`,
  `scripts/cli.mjs`, and the current `tests/` tree.
- Update active domain docs for RSVP, content collections, theme typography, and related evergreen
  architecture references.
- Remove stale commands, missing links, dead routes, and obsolete public API patterns from active
  docs.
