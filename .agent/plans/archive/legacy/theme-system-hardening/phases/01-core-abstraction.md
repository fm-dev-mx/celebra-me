# Phase 01: Core Theme Abstraction & Tokenization

## Tasks
- [ ] Define new semantic tokens for image filters:
    - `--theme-image-filter-hero`
    - `--theme-image-filter-portrait`
- [ ] Update `_editorial.scss` and `_luxury-hacienda.scss` with these tokens.
- [ ] Refactor `_base-theme.scss` to consume these tokens instead of hardcoded values.
- [ ] Verify that all existing presets still render correctly.
